Docker CE (Community Edition)
→ This is the Docker engine that actually runs containers

Docker Compose
→ A tool used to run multiple containers together using a YAML file
(for example: app + database + cache)